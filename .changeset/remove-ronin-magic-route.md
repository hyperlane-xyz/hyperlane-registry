---
'@hyperlane-xyz/registry': patch
---

Removed the ronin leg from the MAGIC warp route, dropping its synthetic token and all connections to it, and renamed the route from `MAGIC/arbitrum-abstract-ronin-base` to `MAGIC/abstract-arbitrum-base`. The universal-router allowlist entry was updated to the new ID.
