---
'@hyperlane-xyz/registry': patch
---

Tron and Tronshasta metadata updated to use HTTP REST API endpoints (`walletUrls`/`walletSolidityUrls`) instead of gRPC endpoints (`grpcUrls`/`solidityGrpcUrls`).
