---
'@hyperlane-xyz/registry': minor
---

Update Hyperlane dependencies to SDK 40.0.0 and Utils 40.0.0 so `check-warp-deploy` runs a CLI that includes the warp-check fixes for synthetic `token` (monorepo #9084) and governance-ICA `ownerStatus` (monorepo #9100/#9104), clearing the false-positive On-Chain violations on the eclipse USDC/USDT synthetic legs.
